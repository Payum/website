export default defineAppConfig({
  ui: {
    colors: {
      // Both palettes are defined in app/assets/css/main.css, so any Nuxt UI
      // component dropped onto a page lands on the Payum theme.
      primary: 'payum',
      neutral: 'ink'
    }
  }
})
